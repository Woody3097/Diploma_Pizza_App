import { AuthLocatorDirective } from './auth-locator.directive';

describe('AuthLocatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthLocatorDirective();
    expect(directive).toBeTruthy();
  });
});
